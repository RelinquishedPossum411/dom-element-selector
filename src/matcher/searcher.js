
import global from "../util/globals";

/**
 * Searches for
 */
export default function (root, tree) {
    for (const child of root.children) {
        // Check the unique properties first: ID, tag name and classes.
    }
}
